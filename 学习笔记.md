# YimMenuV2 代码学习笔记

> 目标：用一个真实的 C++23 项目，学习从"写作业代码"到"写工程代码"的过渡
> 项目：GTA V 的 Mod 菜单（约 685 个源文件）
> 你的起点：会 C++ 语法，写过课程作业，不懂 DLL / Hook / 逆向工程

---

## 零、先把"这个项目到底干了什么"讲清楚

### 你写的 C++ 程序 vs 这个项目

你以前写的程序大概是这样的：

```
你写 main.cpp  →  编译成 .exe  →  双击运行  →  打印结果
```

这个项目完全不同：

```
你写代码  →  编译成 .dll  →  用注入器把 .dll 塞进正在运行的
→  GTA 游戏进程中  →  .dll 在游戏内部画出一个菜单  →  Hook
→  （劫持）游戏函数来改变游戏行为
```

### 三个核心概念（用比喻讲）

| 概念 | 比喻 | 在这个项目里 |
|------|------|------------|
| **DLL** | 不像 .exe 能自己跑，像个插件/模块，必须被人加载才有生命 | 我们要写的代码最终是 YimMenuV2.dll |
| **注入** | 游戏是个跑着的工厂，我们把 DLL 像新工人一样塞进去 | 用注入器把 dll 塞进 GTA5.exe |
| **Hook** | 游戏工厂里有个"扣血"的函数，我们把它改成"先加钱，再扣血，然后回血" | 用 MinHook 库劫持游戏函数 |

**一句话**：写一个 DLL 模块，强行塞进 GTA 进程里，接管它的渲染和逻辑。

### 你已经有 C++ 基础，可以看懂的类比

`main.cpp` 里的这段代码，跟你的作业程序没啥区别：

```c++
// 这就是一个普通函数，里面调用各种类
while (g_Running)           // 主循环 —— 你的作业里也有 while 循环
{
    Settings::Tick();       // 调用一个类的静态方法
    std::this_thread::yield(); // 让出 CPU 时间片
}
```

只不过它的"main"不叫 `main`，叫 `DllMain`（因为编译出来的是 DLL，不是 exe）。

---

## 一、先看 3 个最基础的文件（今天就能搞定）

你不需要一开始就理解全部 685 个文件。先读这 3 个，理解"骨架"。

### 第 1 步：全局头文件（5 分钟）

**文件**：[common.hpp](src/common.hpp)

```c++
// 你熟悉的 include
#include <windows.h>      // Windows API 头文件（以前没用过，但语法一样）
#include <vector>          // STL 容器，你肯定用过
#include <memory>          // shared_ptr 那些

// 你不熟悉但很好理解的 include
#include <imgui.h>         // 这是一个画 UI 的库，就像 cout 输出文字，它输出图形界面
#include <MinHook.h>       // MinHook 库：帮我们做函数劫持的
#include <nlohmann/json.hpp> // JSON 库，读/写配置文件用的

// 全局变量声明（extern，定义在 common.cpp 里）
namespace YimMenu
{
    extern std::atomic<bool> g_Running;  // 控制主循环的开关
    extern HINSTANCE g_DllInstance;      // 这个 DLL 模块的句柄
    extern HANDLE g_MainThread;          // 主线程句柄
    extern DWORD g_MainThreadId;         // 主线程 ID
}
```

**你只需要看懂**：
- 这就是把所有要用到的头文件集中在一个地方（你以前的作业可能也有类似的 `common.h`）
- 声明了 4 个全局变量，控制整个程序的生命周期

### 第 2 步：入口点（15 分钟）★ 最重要

**文件**：[main.cpp](src/main.cpp)

这是整个项目最重要的文件，但代码本身并不复杂。

```c++
// 💡 你知道 main() 是 exe 的入口
//    同样的，DllMain() 是 dll 的入口（当你把 dll 注入到游戏时，Windows 自动调用它）

BOOL WINAPI DllMain(HINSTANCE dllInstance, DWORD reason, void*)
{
    using namespace YimMenu;

    if (dllInstance)
        DisableThreadLibraryCalls(dllInstance);  // 性能优化，不重要

    if (reason == DLL_PROCESS_ATTACH)             // "我的 dll 刚被加载进游戏了"
    {
        g_DllInstance = dllInstance;             // 保存一下自己

        // 💡 为什么不在 DllMain 里干所有事？
        //    因为 DllMain 里有很多限制（不能干耗时操作，不能阻塞等等）
        //    所以只创建一个新线程，在那个线程里做初始化
        g_MainThread = CreateThread(nullptr, 0, Main, nullptr, 0, &g_MainThreadId);
    }
    return true;
}
```

**理解要点**：
- `DllMain` 相当于你 exe 程序里的 `main()` —— 是程序的第一步
- 它只做最少的事：创建线程，然后立刻返回
- 真正的逻辑在 `Main()` 函数里

```c++
DWORD Main(void*)
{
    // === 第一阶段：准备环境 ===
    FileMgr::Init(documents);           // 在磁盘上建个文件夹存配置
    LogHelper::Init(...);               // 初始化日志（你现在有 cout，这是写到文件的 cout）
    Settings::Initialize(...);          // 读 settings.json 配置文件

    // === 第二阶段：连接到游戏 ===
    ModuleMgr.LoadModules();            // 看当前进程里有哪些 dll 模块
    Pointers.Init();                    // ★ 找到游戏函数的地址（后面讲怎么做）
    AnticheatBypass::RunOnStartup();    // 绕过反作弊

    // === 第三阶段：装我们的图形界面 ===
    Renderer::Init();                   // ★ 劫持游戏的 DX12 渲染，叠上我们的 UI

    // === 第四阶段：Hook 游戏函数 ===
    Hooking::Init();                    // ★ 正式劫持游戏函数
    Players::Init();                    // 获取所有在线玩家

    // === 第五阶段：注册功能模块 ===
    ScriptMgr::Init();                  // ScriptMgr 是一个"定时任务调度器"
    FiberPool::Init(16);                // 创建 16 个协程

    // 💡 这里的每一行都是往调度器里注册一个功能：
    ScriptMgr::AddScript(std::make_unique<Script>(&Self::RunScript));     // 更新自身状态
    ScriptMgr::AddScript(std::make_unique<Script>(&GUI::RunScript));      // 画菜单
    ScriptMgr::AddScript(std::make_unique<Script>(&LuaManager::RunScript));// 跑 Lua 脚本
    ScriptMgr::AddScript(std::make_unique<Script>(&Commands::RunScript)); // 处理命令
    // ... 还有更多 ...

    // === 第六阶段：主循环 ===
    while (g_Running)               // 只要没关，就一直循环
    {
        Settings::Tick();           // 保存设置（你改了选项就写入 json）
        std::this_thread::yield();  // 让出 CPU，别独占
    }

    // === 第七阶段：退出清理 ===
    Hooking::Destroy();             // 把所有 Hook 还原（别让游戏崩溃）
    Renderer::Destroy();            // 把 UI 拿掉
    FreeLibraryAndExitThread(g_DllInstance, EXIT_SUCCESS); // 卸载自己
}
```

**这一阶段你的学习目标**：能把这个流程用自己的话说出来。

### 第 3 步：构建配置（5 分钟）

**文件**：[CMakeLists.txt](CMakeLists.txt)

```cmake
cmake_minimum_required(VERSION 3.20.x)       # CMake 版本
project(YimMenuV2 VERSION 0.0.1)             # 项目名

# 引入第三方库（imgui 画 UI，minhook 做劫持，luajit 跑脚本）
include(cmake/imgui.cmake)
include(cmake/minhook.cmake)
include(cmake/json.cmake)
include(cmake/luajit.cmake)

# 把所有 .hpp 和 .cpp 收集起来
file(GLOB_RECURSE SRC_FILES "${SRC_DIR}/**.hpp" "${SRC_DIR}/**.cpp")

# 编译成 DLL 模块
add_library(${PROJECT_NAME} MODULE ${SRC_FILES})
set_property(TARGET ${PROJECT_NAME} PROPERTY CXX_STANDARD 23)  # 用 C++23 标准

# 💡 预编译头：把 common.hpp 预先编译，后续的 .cpp 编译都会更快
target_precompile_headers(${PROJECT_NAME} PUBLIC "${SRC_DIR}/common.hpp")

# 链接依赖库
target_link_libraries(${PROJECT_NAME} PRIVATE
    AsyncLogger        # 日志库
    imgui              # 图形界面库
    minhook            # Hook 库
    dbghelp            # Windows 调试帮助
    # ...
)
```

**关键行**：`add_library(... MODULE ...)` → 输出是 `.dll`，不是 `.exe`

---

## 二、现在看项目怎么组织代码（靠你的 C++ 基础就够了）

### 你熟悉的：单例模式

这个项目大量使用单例。如果你学 C++ 时写过单例，这里一模一样：

```c++
// 项目里的写法（在 Hooking.hpp 中）
class Hooking
{
    static Hooking& GetInstance()
    {
        static Hooking i{};  // 💡 这就是你学过的"懒汉式单例"
        return i;
    }

    Hooking() {}                         // 私有构造函数，外部不能 new
    Hooking(const Hooking&) = delete;    // 禁止拷贝
    // ...

public:
    static bool Init() { return GetInstance().InitImpl(); }
};
```

**每一个**你觉得"应该只有一个"的东西都是单例：`Hooking`（劫持管理器）、`Renderer`（渲染器）、`Settings`（设置管理）、`FileMgr`（文件管理）、`Self`（本地玩家）。

### 你熟悉的：继承

```c++
// Core 层的命令系统就是简单的继承体系
class Command           // 基类
{
    std::string m_Name;
    virtual void OnChange() { }
    virtual void Execute() { }
};

class BoolCommand : public Command     // 布尔开关
    { override Execute() { /* 开关切换 */ } };

class IntCommand : public Command      // 整数滑块
    { /* 有最小值、最大值 */ };

class LoopedCommand : public Command   // 每帧都跑的命令
    { /* 大部分功能是这种 */ };
```

你学过的虚函数、override、基类指针，在这就是日常。

### 你熟悉的：类的封装

```c++
// Player.hpp — 这就是一个普通的 C++ 类，封装游戏里的玩家数据
class Player
{
    CNetGamePlayer* m_Handle;  // 指向游戏内部的对象（相当于一个"遥控器"）

public:
    bool IsValid() const;      // 这个玩家在线吗？
    int GetId() const;         // 玩家编号
    const char* GetName();     // 玩家名字
    Ped GetPed();              // 玩家控制的角色（行人对象）
    bool IsHost();             // 是房主吗？
    int GetRank();             // 等级
    int GetMoney();            // 钱

    void TeleportTo(const rage::fvector3& coords);  // 传送玩家
};
```

**这是什么**：你在 C++ 课上学过"封装"，这就是活生生的例子——用 C++ 类把游戏里的一个复杂对象（玩家）包装成有好几个方法的整洁接口。

---

## 三、逐步攻克"你没学过的概念"

以下是你没接触过的东西。我会先讲原理，再指向对应代码。

---

### 概念 A：Hook 是什么？（最重要，但原理很简单）

#### 先看正常情况

```
到学校：
  路人 → "同学，食堂怎么走？"
  你   → "直走左拐"           ← 正常回答

游戏里：
  GTA引擎 → 调用 GetPlayerHealth()  → 返回血量
```

#### Hook 之后

```
到学校：
  路人 → "同学，食堂怎么走？"
  你被塞了 100 块 →
  你   → "别去食堂了，去旁边海底捞"  ← 被"劫持"了

游戏里：
  GTA引擎 → 调用 GetPlayerHealth()
  但函数开头被改了 → 先跳到我们的代码 → 我们返回 99999
```

#### 技术上怎么实现？

```
原始函数开头的 5 个字节（汇编指令）：
  48 89 5C 24 08  ...

被我们改写成：
  E9 XX XX XX XX     ← 这是一条 JMP 指令，跳到我们的函数

我们的函数里可以：
  1. 干我们想干的事
  2. 调用原始函数（MinHook 帮我们保留了原始的 5 字节）
  3. 修改返回值
```

**对应代码**：[DetourHook.hpp](src/core/hooking/DetourHook.hpp)

```c++
template<typename T>
class DetourHook : public BaseHook
{
    void* m_TargetFunc;   // 目标函数地址（要劫持的）
    void* m_DetourFunc;   // 我们的函数（劫持后跳转到这里）
    void* m_OriginalFunc; // 原始函数的副本（方便我们调用原函数）

public:
    DetourHook(const std::string_view name, void* target, T detour)
    {
        // MH_CreateHook 是 MinHook 库提供的：
        // "帮我在 target 这个地方创建 Hook，执行流改到 detour，原来的保存在 m_OriginalFunc"
        MH_CreateHook(target, detour, &m_OriginalFunc);
    }

    T Original() const  // 获取原始函数的指针，方便我们调用原函数
    {
        return reinterpret_cast<T>(m_OriginalFunc);
    }
};
```

**一句话总结**：Hook = 在某个函数执行前，先执行我们的代码。就像你给游戏函数设了个"代理"。

---

### 概念 B：DLL 注入是什么？

你的程序是 .exe，自己启动自己跑。DLL 则是"被动的"——它不能自己跑，要被人加载。

**注入的过程**（这个项目的使用者做的，不是写代码的人做的）：

```
1. 下载 YimMenuV2.dll（编译产出）
2. 下载一个"注入器"（比如 Xenos）
3. 启动 GTA V
4. 在注入器里选 YimMenuV2.dll
5. 点"注入"

注入器做的事：
  → 打开 GTA V 的进程（OpenProcess）
  → 在 GTA 的进程空间里分配一块内存（VirtualAllocEx）
  → 把 YimMenuV2.dll 的路径写进去
  → 然后强制让 GTA 调用 LoadLibrary("YimMenuV2.dll")
  → LoadLibrary 会调用 DLL 里的 DllMain
  → DllMain 就是我们写的代码（看 entry point 那节）
```

**对于你（写代码的人）**：你只需要关心 `DllMain` 往后的逻辑。注入的事是工具干的。

---

### 概念 C：内存 Pattern 扫描是什么？

问题：GTA V 每次更新后，函数内存地址会变。我们怎么找到"要 Hook 的函数"？

答案：**特征码扫描**——用机器码片段当"指纹"来定位函数。

```
比喻：
  你要找一个同学的课桌，但教室每天都换
  你不记"第3排第2个"，而是找"桌上贴着灌篮高手海报的"那个座位

代码里：
  不记函数地址 0x141A3B2C0，而是找内存里这个模式 →
  48 8B 05 ?? ?? ?? ?? 48 85 C0 74 ?? 48 8B 48  ?? 是通配符，匹配任意字节
```

**对应代码**：[PatternScanner.hpp](src/core/memory/PatternScanner.hpp)

```c++
// 简单理解：在内存的一片区域中，找"气味"一样的字节序列
class PatternScanner
{
    // 比如：在这个模块的内存里，找 "48 8B 05 ?? ?? ?? 48 85 C0" 这段特征码
    // ?? 表示"任意字节都可以"
};
```

**你不需要自己写 Pattern**，但要知道 `Pointers.cpp` 里那几百个 Pattern 就是用来在游戏运行时定位函数地址的。

---

### 概念 D：ImGui 是什么？

你以前输出东西用 `cout << "hello"`，ImGui 用类似的思路输出图形：

```c++
// 你熟悉的
cout << "Hello, World!";

// ImGui 干的事（简单理解）
ImGui::Begin("我的菜单");          // 创建一个窗口
if (ImGui::Button("无敌模式"))    // 画一个按钮
{
    g_GodMode = !g_GodMode;      // 点击后切换状态
}
ImGui::SliderFloat("速度", &speed, 0.0f, 10.0f);  // 画一个滑块
ImGui::End();                      // 窗口结束
```

ImGui 是"立即模式 GUI"：每帧都重新调用这些函数来画 UI。不像 Qt 那样创建控件然后事件驱动。

---

### 概念 E：Fiber（纤程）是什么？

你已经知道线程（Thread）。纤程是"更轻量的线程"：

```
线程：操作系统管理的，有自己的栈。切换线程要进内核态，耗时长。
纤程：用户态的，程序员自己控制切换。成本极低。

为什么需要它？
  GTA V 的脚本系统必须在游戏主线程上执行。
  但我们想让多个功能"看起来同时在跑"。
  
  解决：在主线程上创建多个纤程，轮流执行各个功能的代码。
  写功能的人感觉自己在独享一个线程，实际底层是纤程调度。
```

**对应代码**：[FiberPool.hpp](src/core/backend/FiberPool.hpp)
- 创建 N 个纤程
- 每个纤程上跑一个 Script（功能模块）
- 轮流切换，模拟并发

---

## 四、三个代码分层（按"你会不会 C++"来划分）

```
┌──────────────────────────────┐
│  Core/ （框架层）              │  ← 🟢 你完全可以看懂
│  不依赖游戏，纯 C++ 项目都能用  │     单例、继承、RAII、STL、模板
│  文件：约 80 个                 │     全是你在课上学过的东西
├──────────────────────────────┤
│  Game/ （游戏层）              │  ← 🟡 需要学新知识但代码结构简单
│  依赖 GTA，通过指针操作游戏数据  │     类封装游戏对象，通过 Hook 修改游戏
│  文件：约 150 个                │
├──────────────────────────────┤
│  Types/ （类型定义层）          │  ← 🔴 逆向工程产物，暂时可以不看
│  GTA 游戏内部结构体的 C++ 还原   │
│  文件：约 400 个                │
└──────────────────────────────┘
```

**策略**：先只看 Core，看懂后开始看 Game 里简单的功能。

---

## 五、实际可行的学习路径

### 第一周：在 Core 层练 C++（不用理解 Hook，就看你熟悉的代码）

| 天 | 文件 | 练什么 | 你会的 |
|:---:|------|--------|--------|
| 1 | [common.hpp](src/common.hpp)+[main.cpp](src/main.cpp) | 理解程序入口和主循环 | 头文件组织、while 循环、函数调用 |
| 2 | [DetourHook.hpp](src/core/hooking/DetourHook.hpp) | 模板类、构造析构、RAII | 模板、构造函数、delete |
| 3 | [Command.hpp](src/core/commands/Command.hpp)+[BoolCommand.hpp](src/core/commands/BoolCommand.hpp) | 继承体系、虚函数 | 继承、多态、override |
| 4 | [Self.hpp](src/core/backend/../...) | 但先不看复杂的，看 [Settings.hpp](src/core/settings/Settings.hpp) | 单例模式、JSON 操作 |
| 5 | [File.hpp](src/core/filemgr/File.hpp)+[Folder.hpp](src/core/filemgr/Folder.hpp) | 文件操作封装 | 面向对象、RAII、文件 IO |
| 6 | [Submenu.hpp](src/core/frontend/manager/Submenu.hpp)+[Category.hpp](src/core/frontend/manager/Category.hpp) | UI 树形结构 | 类关系、聚合、vector |
| 7 | 回顾本周，画一张类图 | 把 Command/UI/Hooking 的关系图画出来 | 综合理解 |

### 第二周：理解 Hook 和游戏交互

| 天 | 文件 | 学什么 |
|:---:|------|--------|
| 1 | 原理：找一篇 MinHook 的中文/英文教程看 | 理解 Detour Hook 的汇编原理 |
| 2 | [Hooks.hpp](src/game/hooks/Hooks.hpp) | 游戏 Hook 了哪些函数，为什么 Hook 它们 |
| 3 | [Player.hpp/cpp](src/game/gta/Player.hpp) | 游戏对象怎么封装的 |
| 4 | [Self.hpp/cpp](src/game/backend/Self.hpp) | 本地玩家的单例模式 |
| 5 | [Players.hpp/cpp](src/game/backend/Players.hpp) | 管理所有在线玩家 |
| 6 | 选一个 feature 阅读 | 从 UI 开关到游戏逻辑的完整链路 |

### 第三周之后：自己动手

1. **抄写**一个简单功能（比如 `world/RiotMode.cpp`），一行行理解
2. **修改**一个功能——比如把 Riot Mode 改成其他行为
3. **自己写**一个新功能，按 "定义 Command → 注册到 Submenu → 在 Script 里执行 → 调用游戏函数" 的流程

---

## 六、一个功能是怎么工作的（完整流程）

以"无敌模式"（Godmode）为例，如果你写这个功能：

```
第 1 步：UI 层（菜单）
  在 GUI submenu 里加一项：
    ImGui::Checkbox("无敌模式", ...)
  → 用户按键盘，勾选上

第 2 步：Command 层（逻辑开关）
  BoolCommand 被 toggle
  → command.OnChange() 被调用
  → 标记为"已启用"
  → 保存到 settings.json（下次启动还在）

第 3 步：Looped Script 层（每帧执行）
  每帧 GameTick 时：
    if (godmode_command.IsEnabled())
    {
        Self::GetPed().SetInvincible(true);  // 设置本地玩家无敌
    }

第 4 步：Game API 层（调用游戏函数）
  SetInvincible(true)：
    → 调用 GTA 的 NATIVE 函数 SET_PLAYER_INVINCIBLE
    → 这个函数修改了游戏内存中表示"是否无敌"的那个字节
    → 游戏引擎读到这个值，不再扣血
```

**每一层都是普通的 C++ 代码**，只是底层在与游戏内存交互。

---

## 七、C++ 学习要点（在这个项目里你能复习到的东西）

你在书本上学的东西，在这个项目里都能看到：

| 你学的 | 在这个项目里的位置 |
|--------|-----------------|
| 继承与多态 | `Command → BoolCommand/IntCommand/...` |
| 单例模式 | `Hooking, Renderer, Settings, Self, FileMgr, ScriptMgr, ...` |
| RAII | `MinHook` 包装（构造打开，析构关闭） |
| 模板 | `DetourHook<T>` 模板类 |
| STL 容器 | `std::vector`, `std::map`, `std::unordered_set` 随处可见 |
| Lambda/function | `RendererCallBack = std::function<void()>` |
| 命名空间 | `namespace YimMenu` 整个项目都在里面 |
| constexpr/const | 大量使用，按书上的最佳实践 |
| 智能指针 | `std::unique_ptr`, `std::shared_ptr` |
| string_view | `std::string_view` 替代 `const char*` |
| 移动语义 | `RendererCallBack&& callback` 右值引用 |

---

## 八、当前阶段：你该干什么

### 今天（30 分钟）

1. 读 `common.hpp` —— 5 分钟，看 include 了哪些东西
2. 读 `main.cpp` —— 25 分钟，一行行看，想象自己在写一个 `main()`，只是叫 `DllMain`

### 明天

打开 [DetourHook.hpp](src/core/hooking/DetourHook.hpp)，读这一行：

```c++
MH_CreateHook(m_TargetFunc, m_DetourFunc, &m_OriginalFunc);
```

去搜 "MinHook MH_CreateHook 用法"，看一两篇博客。

### 后面

看看 [Command.hpp](src/core/commands/Command.hpp) 和 [BoolCommand.hpp](src/core/commands/BoolCommand.hpp)，复习继承和多态。

---

## 九、遇到看不懂的词怎么办

| 词 | 对应你学的 |
|----|----------|
| `HINSTANCE` | 就是 Windows 给 DLL 分配的一个编号，类似 `void*` |
| `HANDLE` | Windows 的资源编号，线程/文件/进程都有 HANDLE |
| `DWORD` | 就是 `unsigned long` |
| `BOOL WINAPI` | `WINAPI` 是调用约定（函数参数怎么传），写 dll 才会用到。当成普通 bool 就行 |
| `LPARAM/WPARAM` | Windows 消息参数，当成整数 |
| `reinterpret_cast` | 强制类型转换，你学过 |
| `std::make_unique` | C++14 的创建智能指针，你学过 |
| `goto EARLY_UNLOAD` | 就是 goto，这个项目里用来优雅退出（特殊情况，平时别用） |

---

*最后更新：2026/05/31*
*建议：打开 main.cpp 直接看代码，别被长笔记吓到。代码不咬人。*
